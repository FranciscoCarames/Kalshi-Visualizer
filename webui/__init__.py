"""NiceGUI web UI package (Stage 5) — the opportunity-first dashboard mounted on the FastAPI app.

Named `webui` (not `ui`) so modules can `from nicegui import ui` without shadowing this package.
"""
