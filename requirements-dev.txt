# Dev/test dependencies (not needed to run the app).
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23   # async `user` fixture for the headless NiceGUI browser smoke tests (test_browser.py)
ruff>=0.4
httpx>=0.27   # FastAPI/Starlette TestClient (test_api.py)
# Advisory security sweep (run in the verification step, not a hard test gate): dependency CVEs + static
# analysis. See docs/AUTH.md. `pip-audit` needs network; `bandit` runs offline.
pip-audit>=2.7
bandit>=1.7
